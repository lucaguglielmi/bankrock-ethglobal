{
  "name": "contracts",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "npm run compile",
    "compile": "hardhat compile",
    "postcompile": "node scripts/export-abi.js && node ../scripts/sync-web-abi.mjs",
    "pretest": "npm run compile",
    "test": "hardhat test",
    "deploy": "node scripts/deploy.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "Bank Rock identity and lifecycle registry for physical rocks (Ethereum Sepolia).",
  "dependencies": {
    "@openzeppelin/contracts": "^5.6.1"
  },
  "devDependencies": {
    "hardhat": "^3.16.0",
    "viem": "^2.56.3"
  }
}
