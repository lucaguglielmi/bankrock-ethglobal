# Drafts — not compiled, not deployed, not reviewed

These contracts arrived on `main` after the exit-from-demo-mode audit. Both cover scope that
spec 15 Part 6 cuts from the MVP (ERC-7579 session keys for the agent runtime; idle yield into
a lending protocol). They are kept here, outside Hardhat's `contracts/` source directory, so they
are neither compiled nor included in the deployment set that spec 19 reviews. Promote one back
into `contracts/contracts/` only with a spec section, tests and a spec 19 review of its own.
